# Test dependencies for kubux-mail-client

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# GUI testing (optional - only needed for GUI tests)
pytest-qt>=4.2.0

# Coverage reporting
coverage>=7.3.0

# Async support (if needed in future)
pytest-asyncio>=0.21.0

# Test utilities
pytest-xdist>=3.5.0  # Parallel test execution
pytest-html>=3.2.0    # HTML test reports
pytest-timeout>=2.2.0 # Test timeout enforcement
